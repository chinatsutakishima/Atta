<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        新規トイレ登録
      </h1>
    </div>
  </div>
</section>

<% if @toile.errors.any? %>
  <div class="notification is-danger">
    <h2><%= pluralize(@toile.errors.count, "error") %> prohibited this object from being saved: not successfully</h2>
    <ul>
      <% @toile.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-6">
        <%= form_for @toile do |f| %>
          <div class="field">
            <%= f.label :title, "駅名", class: "label" %>
            <%= f.text_field :title, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :body2, "路線名", class: "label" %>
            <%= f.text_field :body2, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :body3, "綺麗さ", class: "label" %>
            <%= f.text_field :body3, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :body, "詳細", class: "label" %>
            <%= f.text_area :body, class: "textarea" %>
          </div>

          <div class="field">
            <%= f.label :image, "写真", class: "label" %>
            <%= f.attachment_field :image, class: "input" %>
          </div>

          <%= f.submit '投稿', class: "button is-success" %>
        <% end %>
      </div>
    </div>
  </div>
</section>
